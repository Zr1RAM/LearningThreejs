import * as THREE from 'three';
export function applyFordEscapeMaterials(model) {
    // const vehicleMaterial = new THREE.MeshBasicMaterial({
    //     color: 0x383838,
    //     wireframe: true,
    // });
    // model.traverse((child) => {
    //         if (child.isMesh) child.material = vehicleMaterial;
    //     });
}